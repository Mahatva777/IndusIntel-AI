// MainWorkspace — layout orchestrator (architecture §8). Coordinates Digital Twin, Incident Workspace, Right Sidebar, Bottom Panel. Layout orchestrator only (architecture §8).
// Placeholder component — structural only, no data/logic wired yet.
// See README.md in this folder.
export function MainWorkspace() {
  return null;
}
