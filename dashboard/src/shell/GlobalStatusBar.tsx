// GlobalStatusBar — layout orchestrator (architecture §8). Owns plant health summary, incident count, emergency state, connection status, current time. Always visible (architecture §8).
// Placeholder component — structural only, no data/logic wired yet.
// See README.md in this folder.
export function GlobalStatusBar() {
  return null;
}
