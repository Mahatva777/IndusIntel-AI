// BottomPanel — layout orchestrator (architecture §8). Contains Timeline, Workers, Permits, Sensors, CCTV. Collapsible (architecture §8).
// Placeholder component — structural only, no data/logic wired yet.
// See README.md in this folder.
export function BottomPanel() {
  return null;
}
