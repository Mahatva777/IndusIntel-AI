// NavigationRail — layout orchestrator (architecture §8). Owns primary navigation, quick actions, future modules. Persistent (architecture §8).
// Placeholder component — structural only, no data/logic wired yet.
// See README.md in this folder.
export function NavigationRail() {
  return null;
}
