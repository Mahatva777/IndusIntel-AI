// EvidenceChain — P2 per §16.6 (§16.6 (P2), architecture §8 (IncidentWorkspace → EvidenceChain), §9 Evidence Chain).
// Placeholder component — structural only, no data/logic wired yet.
// See README.md in this folder.
export function EvidenceChain() {
  return null;
}
