# Panel — EvidenceChain (P2)

**Spec references:** §16.6 (P2), architecture §8 (IncidentWorkspace → EvidenceChain), §9 Evidence Chain

Owns display of the explainability trail supporting a recommendation — contributing sensors, worker context, permits, maintenance, CV observations, historical precedents (§2 Explainability).

**Scaffold status:** folder and boundary only. No component logic is implemented yet — that is out of scope for this prompt (see project README.md → "Scaffold Only").
