# Panel — SensorPanel (P6)

**Spec references:** §16.6 (P6, 'Metadata and Supporting Panels'), architecture §8 (BottomPanel → SensorPanel), §9 Sensor Panel

Owns raw sensor-level detail as a progressive-disclosure drill-down from telemetry/equipment (architecture §2 Progressive Disclosure).

**Scaffold status:** folder and boundary only. No component logic is implemented yet — that is out of scope for this prompt (see project README.md → "Scaffold Only").
