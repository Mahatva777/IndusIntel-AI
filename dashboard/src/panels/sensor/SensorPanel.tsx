// SensorPanel — P6 per §16.6 (§16.6 (P6, 'Metadata and Supporting Panels'), architecture §8 (BottomPanel → SensorPanel), §9 Sensor Panel).
// Placeholder component — structural only, no data/logic wired yet.
// See README.md in this folder.
export function SensorPanel() {
  return null;
}
