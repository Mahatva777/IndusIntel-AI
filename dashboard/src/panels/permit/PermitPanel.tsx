// PermitPanel — P4 per §16.6 (§16.6 (P4, listed as 'Permit Panel'), architecture §8 (BottomPanel → PermitPanel), §9 Permit Panel).
// Placeholder component — structural only, no data/logic wired yet.
// See README.md in this folder.
export function PermitPanel() {
  return null;
}
