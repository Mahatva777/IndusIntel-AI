// IncidentFocus — P1 per §16.6 (§16.6 (P1), architecture §8 (IncidentWorkspace → IncidentFocus), §9 Incident Focus).
// Placeholder component — structural only, no data/logic wired yet.
// See README.md in this folder.
export function IncidentFocus() {
  return null;
}
