# Panel — IncidentFocus (P1)

**Spec references:** §16.6 (P1), architecture §8 (IncidentWorkspace → IncidentFocus), §9 Incident Focus

Owns display of the active/primary incident under investigation.

**Scaffold status:** folder and boundary only. No component logic is implemented yet — that is out of scope for this prompt (see project README.md → "Scaffold Only").
