# Panel — CCTVPanel (P5)

**Spec references:** §16.6 (P5), architecture §8 (BottomPanel → CCTVPanel), §9 CCTV Panel

Owns camera feed display, driven by domain/camera metadata. Camera frames themselves are never stored in application state (§1.6).

**Scaffold status:** folder and boundary only. No component logic is implemented yet — that is out of scope for this prompt (see project README.md → "Scaffold Only").
