// TimelinePanel — P4 per §16.6 (§16.6 (P4), architecture §8 (BottomPanel → Timeline), §9 Timeline).
// Placeholder component — structural only, no data/logic wired yet.
// See README.md in this folder.
export function TimelinePanel() {
  return null;
}
