# Panel — TimelinePanel (P4)

**Spec references:** §16.6 (P4), architecture §8 (BottomPanel → Timeline), §9 Timeline

Owns the historical-playback / scrub UI, backed by ui-state/timeline. Historical replay is a first-class operational workflow (architecture ADR-011), not a simple chart.

**Scaffold status:** folder and boundary only. No component logic is implemented yet — that is out of scope for this prompt (see project README.md → "Scaffold Only").
