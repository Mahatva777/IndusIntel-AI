// EmergencyBanner — P1 per §16.6 (§16.6 (P1), §10.3 (focus order 1), §9.4–9.6, §9.11).
// Placeholder component — structural only, no data/logic wired yet.
// See README.md in this folder.
export function EmergencyBanner() {
  return null;
}
