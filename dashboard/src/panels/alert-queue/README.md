# Panel — AlertQueue (P3)

**Spec references:** §16.6 (P3), architecture §8 (RightSidebar → AlertQueue), §9 Alert Queue, §7 Alarm Flood Strategy

Owns the prioritized queue of active alerts, subject to the alarm flood/grouping/dedup/suppression rules in §7.

**Scaffold status:** folder and boundary only. No component logic is implemented yet — that is out of scope for this prompt (see project README.md → "Scaffold Only").
