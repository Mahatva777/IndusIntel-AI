// AlertQueue — P3 per §16.6 (§16.6 (P3), architecture §8 (RightSidebar → AlertQueue), §9 Alert Queue, §7 Alarm Flood Strategy).
// Placeholder component — structural only, no data/logic wired yet.
// See README.md in this folder.
export function AlertQueue() {
  return null;
}
