// WorkerPanel — P4 per §16.6 (§16.6 (P4, listed as 'Worker Panel'), architecture §8 (BottomPanel → WorkerPanel), §9 Worker Panel).
// Placeholder component — structural only, no data/logic wired yet.
// See README.md in this folder.
export function WorkerPanel() {
  return null;
}
