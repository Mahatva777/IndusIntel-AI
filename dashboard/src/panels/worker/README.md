# Panel — WorkerPanel (P4)

**Spec references:** §16.6 (P4, listed as 'Worker Panel'), architecture §8 (BottomPanel → WorkerPanel), §9 Worker Panel

Owns workforce context display: who is where, permit assignment, status.

**Scaffold status:** folder and boundary only. No component logic is implemented yet — that is out of scope for this prompt (see project README.md → "Scaffold Only").
