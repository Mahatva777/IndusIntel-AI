# Panel — RecommendationPanel (P2)

**Spec references:** §16.6 (P2, listed as 'Recommendations'), architecture §8 (IncidentWorkspace → RecommendationPanel), §9 Recommendation Panel

Owns display of AI-generated operational recommendations for the active incident. Advisory only — never autonomous (§2 AI as Decision Support).

**Scaffold status:** folder and boundary only. No component logic is implemented yet — that is out of scope for this prompt (see project README.md → "Scaffold Only").
