# §3.4 — every request/stream requires authentication; §4 — streaming auth
# reuses the same authenticated session. Exact auth provider is an Open
# Question in DASHBOARD_ARCHITECTURE.md ("Infrastructure" / "Authentication
# provider") — left undefined here on purpose.

VITE_API_BASE_URL=
VITE_WS_BASE_URL=
