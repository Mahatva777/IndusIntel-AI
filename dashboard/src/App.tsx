import { DashboardShell } from "@shell/DashboardShell";

// Scaffold only. DashboardShell is a structural placeholder — it lays out
// no panels, owns no state, and streams nothing yet. See README.md.
export default function App() {
  return <DashboardShell />;
}
